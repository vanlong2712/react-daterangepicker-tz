import * as React from "react";
import { getCalendarBuilder, THIS_MONTH, THIS_YEAR } from "./core/Calendar";

export const DateRangePicker = () => {
  console.log(getCalendarBuilder());
  return <button>Boop</button>;
};
